Repository is where we store the files and their versions
It has a few layers:
1 - Files in your system
2 - Files added to next commit (staging) -> `git add`
3 - Files commited to local repository -> `git commit -m message`
4 - Files pushed to cloud -> `git push`

